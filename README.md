Dynamic Weather Application

This project is a single-page React weather application that displays current conditions and a 3-day forecast for a specified city.

It uses a custom API hook to fetch weather data asynchronously. 
